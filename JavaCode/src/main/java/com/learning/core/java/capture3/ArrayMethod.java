package com.learning.core.java.capture3;

public class ArrayMethod {

    /**
     * 数组
     */
    public static void main(String[] args) {

        //数组
        //注意最后一个字段允许有逗号；
        int[] smallPrimes = {1,2,3,};

        //匿名数组,这个和上面的是一样的；
        smallPrimes = new int[]{1,2,3};

        //访问数据元素
        //创建一个数组的时候，所有元素都初始化0，boolean类型的会初始化为1，然后对话数组会初始化为null;表示这些元素还没存放对象；
        String[] strings = new String[10];




    }
}
